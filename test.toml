[log]
level = "debug"

[rf]
freq_offset = 0
rx_gain = 40.0
tx_gain = 40.0
srate = 23.04e6
frequency = 1842.5e6
nof_prb = 106
N_id = 1
ssb_numerology = 0

# UHD Configuration (USRP Hardware)
# device_name = "uhd"
# device_args = "type=b200"

# ZMQ Configuration (Network-based simulation)
device_name = "zmq"
device_args = "tx_port=tcp://*:2000,rx_port=tcp://localhost:2001,id=enb"

# File-based Configuration (read from IQ files)
# device_name = "file"
# file_path = "/path/to/rx_file.cf32"

[cell]

[prach]
config_idx = 1
is_nr = true
hs_flag = false
root_sequence_index = 1
zero_correlation_zone = 0
num_ra_preambles = 64
time_delay = 1 # in millisecond

# MSG2 Configuration for 5G RACH Security Research
[msg2]
max_rnti_candidates = 200
search_space_id = 1
coreset_id = 0
enable_multiple_rnti = true
msg2_window_ms = 4

